{
  "name": "finpay",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix frontend --legacy-peer-deps && npm run build --prefix frontend && npm install --prefix backend --legacy-peer-deps && cd backend && npx prisma generate",
    "start": "node backend/src/index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
